{% extends 'base.html.twig' %}

{% block title %}Home | {{ parent() }}{% endblock %}

{% block body %}

    <div>
        <p>Date du jour :  {{ "now"|date("d-m-Y") }}</p>
        <p>Participant : {{ participant.prenom|capitalize }} {{ participant.nom|first }}.</p>
    </div>

    <div class="example-wrapper">
        <h1>Page :  PAGE ACCUEIL </h1>


            <div class="infos">
                <table>
                    <tr>
                        <th>Nom de la sortie</th>
                        <th>Date de la sortie</th>
                        <th>Clôture</th>
                        <th>inscrits/places</th>
                        <th>Etat</th>
                        <th>Inscrit</th>
                        <th>Organisateur</th>
                        <th>Actions</th>
                    </tr>
                    {% for sortie in sorties %}
                    <tr>
                        <td>{{ sortie.nom }}</td>
                        <td>{{ sortie.dateHeureDebut|date("d-m-Y") }}</td>
                        <td>{{ sortie.dateLimiteInscription|date("d-m-Y") }}</td>
                        <td>0/{{ sortie.nbInscriptionsMax }}</td>
                        <td>En cours</td>
                        <td>Oui</td>
                        <td>{{ sortie.organisateur.nom }} {{ sortie.organisateur.prenom|first }}.</td>
                        <td>Modifier - Annuler</td>
                    </tr>
                    {% endfor %}
                </table>
            </div>

    </div>
{% endblock %}

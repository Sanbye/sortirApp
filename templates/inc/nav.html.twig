<nav>
    <ul>
        {% if app.user %}
            <li><a href="{{ path('sortie_list') }}" title="Accueil">Accueil</a></li>
            <li><a href="{{ path('participant_monProfil') }}" title="Mon profil">Mon profil</a></li>
            <li><a href="{{ path('participant_logout') }}" title="Se déconnecter">Se déconnecter</a></li>
        {% endif %}

        {% if is_granted("ROLE_ADMIN") %}
            <li><a href="{{ path('admin_campus') }}" title="Campus">Campus</a></li>
        {% endif %}

    </ul>
</nav>